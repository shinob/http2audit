# nginx_safe.conf - 推奨値が設定済み（HTTP/2 無効）
http {
    http2_max_concurrent_streams 64;
    large_client_header_buffers 4 8k;
    client_header_timeout 10s;
    keepalive_timeout 15s;

    server {
        listen 443 ssl;
    }
}
