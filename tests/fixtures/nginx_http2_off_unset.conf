# nginx_http2_off_unset.conf - HTTP/2 無効・http2_max_concurrent_streams も未設定
# (HTTP/2 を使っていないサーバーで http2 専用設定が未設定なだけのケース)
http {
    large_client_header_buffers 4 8k;
    client_header_timeout 10s;
    keepalive_timeout 15s;

    server {
        listen 443 ssl;
    }
}
